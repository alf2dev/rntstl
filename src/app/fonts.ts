import { Reenie_Beanie } from 'next/font/google';
import Days_One from 'next/font/local';

export const reenie_beanie = Reenie_Beanie({
  weight: '400',
  variable: '--my-font',
});

export const days_one = Days_One({
  src: './fonts/DaysOne-Regular.woff2',
  variable: '--font-days-one',
});
